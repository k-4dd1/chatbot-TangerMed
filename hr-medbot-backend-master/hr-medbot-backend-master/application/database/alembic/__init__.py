"""
to create the tables, run the following command:
alembic init alembic

to create the tables, run the following command:
alembic migrate


after updating the models, run the following command to update the database:
alembic revision --autogenerate -m "message"
alembic upgrade head


"""